# Django
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database
# Leave DATABASE_URL unset to use SQLite for local development.
# DATABASE_URL=postgres://agentic:agentic@db:5432/agentic_django
# Optional: override the default SQLite path (used when DATABASE_URL is unset).
# SQLITE_PATH=/data/db.sqlite3

# Tasks backend selection
TASKS_BACKEND=django_tasks.backends.rq.RQBackend

# Redis / RQ
REDIS_URL=redis://redis:6379/0

# OpenAI
OPENAI_API_KEY=your-key-here
OPENAI_DEFAULT_MODEL=gpt-5.2
